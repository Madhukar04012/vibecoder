# Frontend (NOVA AI Assistant)

This is the primary web UI for VibeCober.

## Run locally

```bash
npm install
npm run dev
```

This serves the **landing page build** from `frontend/dist`.

Then open `http://localhost:5173`.

## Run the IDE app (Next.js)

```bash
npm run dev:ide
```
